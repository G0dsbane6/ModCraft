services:
  - type: web
    name: modcraft
    env: node
    nodeVersion: 22
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
